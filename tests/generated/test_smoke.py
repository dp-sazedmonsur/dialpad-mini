import pytest

def test_smoke():
    """
    Simple smoke test to ensure pytest + Playwright env works.
    This will always pass, acting as a placeholder until AI-generated
    tests are added via PR automation.
    """
    assert True